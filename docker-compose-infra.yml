version: '3.8'

services:
  # MySQL 服务
  mysql:
    image: mysql:8.0
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    environment:
      MYSQL_ROOT_PASSWORD: admin123
      MYSQL_DATABASE: doc_ai
    volumes:
      - mysql-data:/var/lib/mysql
      - ./mysql-schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
    networks:
      - docai-network
    ports:
      - "3306:3306"

  # Redis 集群
  redis:
    image: redis:7.0
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: on-failure
    command: redis-server --requirepass redis123 --appendonly yes
    volumes:
      - redis-data:/data
    networks:
      - docai-network
    ports:
      - "6379:6379"

  # RabbitMQ
  rabbitmq:
    image: rabbitmq:3.8-management
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: rabbit123
    networks:
      - docai-network
    ports:
      - "5672:5672"
      - "15672:15672"

  # MinIO
  minio:
    image: minio/minio
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: minio123
    command: server /data --console-address ":9001"
    volumes:
      - minio-data:/data
    networks:
      - docai-network
    ports:
      - "9000:9000"
      - "9001:9001"

  # Nacos
  nacos:
    image: nacos/nacos-server:v2.2.0
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    environment:
      MODE: standalone
      NACOS_AUTH_ENABLE: "false"
      SPRING_DATASOURCE_PLATFORM: mysql
      MYSQL_SERVICE_HOST: mysql
      MYSQL_SERVICE_PORT: 3306
      MYSQL_SERVICE_DB_NAME: nacos_config
      MYSQL_SERVICE_USER: root
      MYSQL_SERVICE_PASSWORD: admin123
      MYSQL_SERVICE_DB_PARAM: characterEncoding=utf8&connectTimeout=30000&socketTimeout=30000&autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true
    ports:
      - "8848:8848"
      - "9848:9848"
      - "9849:9849"
    volumes:
      - nacos-data:/home/nacos/data
    networks:
      - docai-network

networks:
  docai-network:
    external: true

volumes:
  mysql-data:
  redis-data:
  minio-data:
  nacos-data: